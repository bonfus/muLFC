#ifndef UTILITY_H
#define UTILITY_H
#include <stdio.h>
#include "mat3.h"
#include "vec3.h"

void print_vec3(const char * msg, const vec3 * i);

void print_mat3(const char * msg, const mat3 * i);

#endif
